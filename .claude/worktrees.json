{
  "setup-worktree-unix": [
    "uv sync",
    "prek install --prepare-hooks -t pre-commit -t commit-msg -t prepare-commit-msg",
    "git config commit.template .gitmessage",
    "test -f \"$ROOT_WORKTREE_PATH/.env\" && cp \"$ROOT_WORKTREE_PATH/.env\" .env || true"
  ]
}
