# trycmd runs the walkthrough in-place; only study.tsra is a real input.
# The key, sidecar, and trust store are generated by the walkthrough and must not be tracked.
/instrument.key
/instrument.key.pub
/study.tsra.sig.json
/.tessera/
