# Tessera — Architecture Decision Records

Each ADR records one load-bearing decision: context, the decision, why, and consequences. They
correspond to the open-decision register in `tessera/docs/ROADMAP.md` (D1–D7). Status: Proposed /
Accepted / Superseded.

| ADR | Decision (register id) | Status |
|---|---|---|
| [0057](0057-ingest-topology-thin-waist-and-release-matrix.md) | Ingest **topology** — the packaging axis of ADR-0056 (#398): *where decoders live, what compiles into which binary, what ships*. **The waist is `ColumnData`/`ArrayData` in `tessera-io`, NOT `tessera-core`** — core owns the primitive spec and takes **no `arrow` dep** (it is the wasm crate's only dep + the pure-abi3 wheel's); canonicalisation lives in `tessera-ingest` behind an `arrow` feature. **`tessera-ingest` stays ONE crate** with feature-gated modules + four named extraction triggers (N crates.io coordinates + N lockstep versions + an N×features CI matrix are irreversible for zero present benefit). **cargo-dist CAN emit two feature-differentiated binaries — PROVEN by probe (one `cargo build --package=X` per app; `bin-aliases`; distinct bin names mandatory) — and we DECLINE it**: it saves no CI (`static-hdf5` still runs), forks `curl \| sh`, and strands the locked-down clinical VM that cannot rebuild; lean/full is a **source + nix** axis, the broad channel ships ONE full binary. **Probe found the live determinism hazard**: `--features sql` already flips `arrow-array/chrono-tz` in the shared arrow tree = ADR-0056 hazard **H1** arriving through a *feature* not a host (harmless only until #386 lands) → **Gate A** (goldens byte-equal across 4 feature configs; verified today) + **Gate B** (`cargo tree` feature-snapshots of seal-path crates, catches `vortex-btrblocks/alp` re-registration *before* it moves a hash). Spec engine: **closed *and total* enum, feature-gate the handler** — `#[cfg]` variants would make a TOML spec's meaning build-dependent and break `spec_hash` portability; `inventory` fails green under `lto=thin`. Mandates `tessera info`, two-exit-code missing-backend errors, and the `IngestOutput` **seam** before #386 | **Proposed** |
| [0056](0056-generic-ingest-normalise-vs-preserve.md) | Generic ingest — the **normalise-vs-preserve ladder** (#386): extends the ADR-0025 normalising path from vendor formats to Parquet/Arrow/NumPy so non-vendor data reaches the primitives instead of falling onto `blob` (the AX write-your-own-data gap, #390). **Route by the data's SHAPE, never auto-decide semantics** (`--as` overrides; advisories suggest, never gate). **Rejects the proposed "lossy-map-with-warning" tier** — a warning does not travel with the artifact and a seal over degraded values asserts they are the truth; three lanes instead: clean · **lossless-with-a-transform recorded INSIDE the seal** · reject-with-an-actionable-error (decimal→`i64`+`scale` never `f64`; dictionary→values dropping producer-specific codes; struct→dotted flatten, list/map→reject since exploding changes row cardinality). **The determinism gate is the `arrow → primitive` canonicalisation, not our encode** (tzdb is a *filesystem* dep; NaN payloads; values-under-null; `=` pins + three-producer hash-equality corpus). Four additive corpus-neutral format fields: `Column.referencing` (the missing ADR-0046 epoch slot), sealed `ingest_decoder`, `ingest_transform`, `Column.sensitivity` + `Sensitivity::Unknown` (`Public` would seal a false safety claim on a patient CSV). **One verb axis — the primitive**: `ingest table\|array\|blob` + `--from <backend>`; vendor verbs deprecated into cookbook recipes (#389). CSV deferred (sample-based inference is anti-deterministic); TIFF cut from P1 (#394); directories = the shape rule one level up (#393). **Amended by #403 (§6a) — the decoder identity is a RECIPE fact, not a format field; §6 drops to three sealed fields.** Two axes. **(i) Where:** the existing **sealed provenance recipe bag** (ADR-0058 `Generation`), well-known key `ingest_decoder` — inside the seal, but **adding no format surface**. A bespoke field was rejected (a 2040 pyarrow/Julia writer cannot derive a Rust-toolchain triple); *record nothing* (R0) was refuted on facts — `Producer.version` is the **format** version, `git_commit` is `None` in the Nix sandbox that IS the canonical build, and `Cargo.lock` pins versions but **not the features a binary was built with**, which is the exact axis ADR-0057 §5 found the hazard on; unsealed `aux/` was rejected because a recipe is a **reproduction contract** and one that names the energy window but omits the decoder is knowingly incomplete on the dimension most likely to move values. **(ii) What:** a mechanically derived **triple** (name + `=`-pinned version + resolved-feature digest) — never a profile id, which is an unbounded maintainer claim whether it sits in a field or a bag. Because ingest is a **logical re-encode**, `content_hash = f(extracted values, our encoder)`, so §6.2's motivating sentence ("a `cargo update` produces a different `content_hash`") is **false as stated** and a value-preserving bump moves only `manifest_hash` — legitimate, since a recipe change *is* a seal change. Detection was already sealed anyway via the `ingested_from` source digest + `content_hash`. **Sequencing:** the provenance model landed on `dev` as ADR-0058 via #409 (superseding stranded PR #346; the ADR-number collision was resolved by renumbering) — ingest can now realise the recipe-bag decoder key against a trunk-visible `Generation` | **Proposed** |
| [0055](0055-whole-file-encryption.md) | Whole-file encryption — `tessera share --to <name>` (seal → `age`/X25519 → `.tsra.age`) / `tessera open` (decrypt → verify), the **third crypto axis**: A=whole-file-to-recipient (grant) vs B=per-field (ADR-0040/0041) vs C=crypto-shred (revoke); A and C are opposite intents, A and B complementary. Encryption strictly **outside the seal** (decrypt-then-verify, same lesson as ADR-0037 "sign the envelope"); **one age envelope core** reused with per-field; **X25519 keys distinct from ed25519** but enrollment **authenticated by the paired ed25519** (spike H4). A cryptography adversarial pass drove **encrypt-then-sign the outer envelope binding the recipient set** (closes signature-demotion / sender-spoof / Davis-forwarding H1–H3) + domain-separation tag; honest cost: whole-file is an **opaque blob → loses cloud prune-before-fetch** (use per-field for cohorts), not erasure, not revocable (ADR-0041 rung). Builds on ADR-0037/0040/0041 | **Proposed** |
| [0054](0054-derived-caches-and-the-spine-block-boundary.md) | Derived metadata caches + the spine/block boundary (#365) — the **sealed JSON spine stays canonical** (Vortex has no canonical form → a codec upgrade would break every seal; block digests are byte-exact, deliberately not codec-agnostic); **bulk tabular metadata → a Vortex table block** (homogeneous rows + large); every columnar/joined/indexed form is an **unsealed derived cache stamped with `built_from`** (stale ⟺ hash mismatch, never authoritative); **RDM presentation = a content-addressed projects→items catalog, not S3 tags**, with catalog columns **projected from the registered `ProductSchema`** (the 1-schema-of-many guard); tooling (`info`/`ls`/`edit`) is the `git cat-file -p` surface. Extends ADR-0042 | **Proposed** |
| [0052](0052-versioning-and-release.md) | Versioning policy & release pipeline — **two axes** (software SemVer from `0.1.0-alpha.1` vs the format `tessera_version` staying `v0`/pre-1.0); **unified workspace version**; **release-plz** (Rust-native) drives version+CHANGELOG from conventional commits as a **held release PR** (nothing tags/publishes until deliberately merged); crates.io/PyPI/binaries deferred to when creds are set. `dev`=moving-target / `main`=distribution | **Accepted** (process; adr-matrix-exempt) |
| [0020](0020-canonical-encoding-and-identity.md) | **D4** canonical encoding (RFC 8785 JCS) + **D5** identity model (id / content_hash / manifest_hash) + manifest & BlockRef schema | **Accepted** |
| [0022](0022-versioning-and-container.md) | **D1** fd5 supersession (done) · versioning DAG · `.tsra` container spec | **Accepted** |
| [0023](0023-array-block-payload.md) | Array block payload — Zarr v3 + pcodec, serialized as one deterministic blob (P3/S5) | **Accepted** |
| [0024](0024-table-block-payload.md) | Table block payload — a single deterministic Vortex file (P3/S5) | **Accepted** |
| [0025](0025-ingest-model.md) | Ingest model — normalise at the door, lossless native dtype, provenance-rooted (P5) | **Accepted** |
| [0026](0026-streaming-table-ingest.md) | Streaming chunked table writes for >RAM ingest — one always-chunked deterministic encoder (#208) | **Proposed** |
| [0027](0027-sub-block-merkle-chunk-index.md) | Sub-block Merkle + content-addressed chunk-index block — per-chunk confirmation + pruning (#214) | **Superseded by 0028** (design shipped as 0028 §3) |
| [0028](0028-unified-hierarchy.md) | The unified hierarchy — recursive MMR Merkle + multiscale `{hash,stats}` pyramid + derived sidecars + fused streaming (#215); supersedes ADR-0020 flat root | **Accepted** |
| [0029](0029-data-model-composition.md) | Data model — composition over inheritance · N-D blocks · multi-dimensional acquisitions (A vs B) · ROI representation · substrate-by-nature (#216) | **Accepted** |
| [0030](0030-spatial-referencing.md) | Spatial referencing — one voxel→world affine + named frame; spacing & OME-Zarr per-level transforms derived; registration = provenance edge; LPS canonical (#217) | **Accepted** |
| [0031](0031-sparse-representation.md) | Sparse data — COO table for scatter (reuses Merkle/stats/pushdown) · dense-chunked + stat-prune for block sparsity · threshold measured · materialize-don't-densify (#218) | **Accepted** (as-built) |
| [0032](0032-referenced-coordinates-and-quantities.md) | Referenced coordinates & quantities — one `(transform, unit, frame)` descriptor unifying space/time/intensity/parametric; UCUM units; store-don't-compute; generalises ADR-0030 (#219, #220) | **Accepted** |
| [0033](0033-collections-and-derivation.md) | Collections & raw→derived boundary — flat content-addressed products, logical collections nest by reference, one descriptor → 3 projections (RO-Crate · OCI-index · S3-prefix), raw=Compliance-WORM / derived=Governance-WORM (#223) | **Proposed** |
| [0034](0034-runtime-parallelism-wasm.md) | Runtime, parallelism & wasm boundary — sync API; std-thread/rayon encode (never tokio); single-block parallelism = multi-block not tokio; tokio reserved for read-side object-store (#225); Vortex is wasm-capable, zstd feature-gated (#224) | **Proposed** |
| [0035](0035-declarative-ingest-spec.md) | Declarative ingest spec + general engine — format-tagged TOML drives a closed-backend dispatcher; spec_hash on each MEMBER as `ingested_via_spec` (no collection format change); streaming-vs-batch heuristic; `--workers`/`--ram-budget`/`--auto` stay CLI-only; v1 derived_from is in-spec | **Proposed** |
| [0036](0036-versioning-audit-object-store.md) | Versioning & audit — content-addressed `objects/<digest>` store + `refs` realizes ADR-0022 CoW (copy ∝ delta); git-shaped verbs (`commit`/`log`/`diff`), `publish`=`git archive` (history-free) vs `seal`=`git bundle` (with history); cloud-native + WORM; records why **not** embed git/git-lfs | **Proposed** |
| [0037](0037-signing-trust-model.md) | Signing trust model — 4-persona user story (instrument→data-scientist→custodian→publisher); fixes 2 bugs (sign the **envelope** not the seal → `ed25519-env-v1`; carry the sidecar through `push`/`pull`); archival principle (verify offline forever); mechanism ladder (ed25519+trust-store alpha · sigstore-bundle+SLSA feature-gated · ssh-agent/HSM on demand); ORCID = attribution not trust; signatures-as-hashed-block = the one format fork | **Proposed** |
| [0038](0038-blob-opaque-preservation.md) | Blob block — opaque, bit-faithful preservation tier (`BlockKind::Blob`): store an un-parsed vendor file (Siemens `.l64`, GE `.7z`, PDF) **verbatim**, digest = `blake3(bytes)`, riding the existing seal/signature/verify unchanged; `format="blob"` (alias `junk`) ingest + `tessera extract`; FAIR F/A/R + integrity but **not Interoperable** until decoded — complements, never replaces, normalised array/table products | **Accepted** (as-built) |
| [0039](0039-compute-node-caching.md) | Compute-node caching — two tiers, both *infra not format* (#236): (A) lightweight = a Tessera versioning repo as the cache (`objects/<digest>` de-dups for free, `tessera gc` + atime-LRU sweep); (B) registry-grade = `zot` pull-through cache with `extensions.sync onDemand` + `storage.retention.policies.keepTags.pulledWithin` (access-recency, the predicate MinIO ILM lacks); standalone example flake + `nixosModules.tessera-cache`; one new in-scope verb `tessera get <ref>→path` (cache-agnostic fetch); explicit non-decision = caching is NOT a `tessera` subcommand | **Proposed** |
| [0040](0040-data-protection-and-confidentiality.md) | Data protection — schema-driven **sensitivity tiers** on `FieldSpec` (`public`/`coded`/`sensitive`/`identifying`, versioned + PS3.15-seeded; DICOM = a transitional special-case of the blob tier); consent-withdrawal **erasure** (destructive `redact` via forget/gc → lineage tombstone · crypto-shred via key-deletion for shared copies); **field-level confidentiality** (`age`/X25519 envelope-encrypt sensitive fields to recipient pubkeys → "coded/anon by default if leaked", sidecar to keep the seal deterministic; revisits ADR-0007 for the share/leak threat model) | Proposed |
| [0042](0042-self-contained-tsra-aux-members.md) | Self-contained `.tsra` — non-sealed `aux/` members (`.gitignore` for the seal): embed the ADR-0037 signature at `aux/signatures/<key_id>.sig.json` (default; `--sidecar` keeps the detached form) and stamp `aux/provenance.json` (RFC-3339 `ingested_at` + producer + host) at ingest. Load-bearing invariant: adding/removing `aux/` members leaves `content_hash` + `manifest_hash` byte-identical, so the conformance corpus is untouched and writer-determinism holds; wall-clock rides outside the seal by construction. Completes ADR-0037 §0 bug #2 (signature carriage) + closes #259 | Proposed |
| [0041](0041-provider-mediated-field-decryption.md) | Provider-mediated field decryption — **rung 2** of ADR-0040's confidentiality ladder (#241): per-tier DEK encrypts the field; **KEK held in an external provider** (KMS / Vault Transit) and accessed via an online `unwrap` handshake → time-gating + revocation + audit + **crypto-shred at scale** (the consent-withdrawal endgame that finally reaches leaked copies); recommends **unwrap-DEK over decrypt-as-a-service** (smaller compliance surface, lower latency, provider never sees PHI) and a **thin Tessera key-broker fronting a real KMS** (provider stays *infra*, not baked into the format — the `.tsra` carries only an opaque provider ref + wrapped-DEK + policy handle); reuses ADR-0040 §4's additive sidecar so writer-determinism is preserved (the seal still commits to plaintext / per-field commitment); **honest analog-hole limit** — provider gates key release + access over time, NOT what an authorized recipient does with decrypted plaintext (screenshot / memory dump / re-export); TEE/confidential-computing named as the partial rung 3 not in scope here | Proposed |
| [0043](0043-unified-recursive-hierarchy.md) | Unified recursive hierarchy (#215) — chunk-index (#214), multiscale pyramid + projections (#260), aux sidecars (#259/ADR-0042), collections (#223), composition/N-D (#216) are **the same node at different scales**: every level (chunk · block · pyramid-level · product · collection) carries a `{hash, stats, referencing}` triple, so verify/prune/summarise/reference are **one set of operations parameterised by scale**, not five bespoke paths; `{hash,stats}` are **byproducts of the streaming write** (fused pyramid + chunk-index + Merkle in one pass); draws the sealed-vs-`aux/` line the same way at every scale (ADR-0042). Synthesis ADR — no new format bytes; pins the invariant the children implement | Proposed |
| [0044](0044-sparse-data-representation.md) | Sparse data (#218) — pins the decision omnibus-recorded in ADR-0031: **COO table for scatter** (Vortex table with `shape`/`dtype`/`fill_value` metadata, canonical lex row order — reuses Merkle/monoid-stats/pushdown/SQL unchanged) · **dense-chunked array + `count=0` prune** for block-sparsity (free from ADR-0028) · **no bespoke sparse codec** (would fork integrity/stats/prune paths) · the lever is **materializability + access pattern**, not occupancy % (bench #221-A) · single-linear-index emit is built, multi-axis emit is a documented extension (gap) | Proposed |
| [0045](0045-units-and-quantities.md) | Units & quantities (#219) — pins the units half of ADR-0032: **UCUM** codes on `ArraySpec.unit`/`FieldSpec.unit` + `_vocabulary`/`_code` escape for domain codes (DICOM/RadLex/SNOMED) · **quantity = raw storage + `(rescale_slope, rescale_intercept, unit)` transform**, physical recovered on read (store-don't-compute; lossless ints round-trip; CT→HU, PET→Bq/mL) · derived physical quantities (SUV, ADC) are **products** with `derived_from` edges, not descriptors · no in-format units engine — hand off to a downstream UCUM library · **gaps:** UCUM validation not enforced (needs `--strict-units` lint), coded-vocabulary values not cross-checked (`--meta coded` lint) | Proposed |
| [0046](0046-time-model.md) | Time model (#220) — pins the time half of ADR-0032: **RFC-3339 UTC** for `Manifest.timestamp` (identity-key, sealed) and `aux/provenance.json:ingested_at` (ADR-0042, outside the seal — writer-determinism) · **elapsed = quantity** via `Referenced::time_regular/time_irregular` (unit `s`, frame `epoch:<instant>`) · **event times = integer ticks + scale** via `time_ticks(period_s, start_s)` (lossless, compact, no float drift — closes the review's "listmode `ms` column has no described semantics" gap) · absolute instant = elapsed + `epoch:<instant>`; canonical epochs `unix`/`acquisition_start`/`injection` · monotonic for intervals, UTC only for stamps · **gaps:** listmode schema not yet carrying the descriptor · cross-block epoch coherence not verified · UTC anchor for `epoch:<instant>` resolutions not yet pinned in the vocabulary | Proposed |
| [0047](0047-offline-crypto-shred-deidentification.md) | Offline crypto-shred de-identification (#269) — the buildable **rung 1** of ADR-0040/0041: `tessera ingest dicom --crypto-shred --recipient <age-pubkey>` de-identifies the sealed product AND `age`-encrypts the stripped identity (full DICOM header + curated identifying fields) to the recipient pubkey(s), carried as an `aux/identity/identity.age` envelope **inside** the one shareable `.tsra`, **outside** the seal (ADR-0042 → determinism intact, corpus untouched). `tessera reidentify --identity <age-key>` recovers it ("a key un-SOPs itself"); `tessera shred` drops the envelope and destroying every key copy erases it from every distributed copy (ADR-0040 §2.2). Completeness by construction (encrypt the whole header, allowlist the safe fields — not a blocklist). Also fixes the #269 raw-header leak: `extra[dicom_header]` is now embedded **only** on a de-identified path. `tessera keygen --age` mints the keypair. NOT rung 2 (no provider/KMS/broker) — offline `age` only | **Accepted** |
| [0048](0048-provenance-edge-shape.md) | Provenance edge shape (#267) — the sealed edge stays a **single merkle-rooted reference** `{role, reference, content_hash=blake3-merkle-over-source-files}`, NOT a list of per-file paths: a path list in the seal would (1) **regress PHI** (890 patient-bearing DICOM paths back in the manifest — re-opening the leak `--source-label`/ADR-0040 closed) and (2) force a **schema break + corpus regen** to carry info that is either deliberately redacted or already committed cryptographically as the merkle root. Per-file **structure**, when needed, rides a PHI-safe `aux/sources/<role>.json` (digests + sizes, **never paths**; outside the seal per ADR-0042 → corpus-neutral; `merkle_root` reconciles with the sealed `content_hash`). Export half already shipped (#275 opaque `urn:tessera:source:<merkle>`); this fixes the data-model half by deciding **not** to bloat the seal. Aux emission = a clean additive follow-up | **Accepted** |
| [0049](0049-recursive-collections.md) | Recursive collections — the nesting *mechanism* (amends ADR-0033 §2, spike #293): one `members` list + a `kind: Product\|Collection` discriminator (bare `#[non_exhaustive]` enum, skip-on-default → `manifest_hash` corpus-neutral, refuse-unknown gated by `tessera_version`); **domain-separated MMR leaf** `BLAKE3(domain ‖ kind ‖ manifest_hash)` so inclusion proofs are kind-bound (accepts a one-time versioned `content_hash` regen); **recursive offline `verify`** (dispatch on kind, cycle-guard = defense-in-depth not security); typed `add_product`/`add_subcollection` handles, `derived_from` peers-only; `kind` authoritative (not filename-suffix); **engine stays level-agnostic** — an open domain-supplied `level` tag, no `exam/cohort/study` enum (RFC §12). Level *semantics* → #294 | **Proposed** |
| [0050](0050-collection-schemas.md) | Collection level schemas (#294) — terminology as **registered, versioned data**, symmetric with `ProductSchema`: a `Collection.collection_schema` (sealed; default permissive `collection`) resolves a `CollectionSchema {member_rule}` the engine validates at seal + verify. Core ships `collection`/`dataset`(products-only)/`project`(sub-collections, recursive); a **domain registers its own** opinionated levels (`exam`/`cohort`/…) — unlimited named levels via schema, zero new engine machinery (each is a `kind:Collection` named+ruled by its schema). `acq`/`derived` = `Role`, not a level. Engine stays generic (2 kinds + recursion); all opinion lives in registered schemas. Amends ADR-0033, relates ADR-0049. P1: `allowed_member_schemas`, embedding, `id_inputs`-binding, domain profile | **Proposed** |
| [0051](0051-listmode-hdf5-decomposition.md) | GE listmode HDF5 decomposition (#305) — the ~120 GB "singles" monolith's 11 datasets become **one product (`.tsra`) per table**, bound by an acquisition **Collection** (ADR-0033/0049). Driven by fetch-granularity: OCI push = one whole-blob sha256, so a **pruned fetch's integrity dies** at the layer → the **prune boundary = the artifact boundary** (pull only `events`, leave 81 GB `singles`, integrity intact per artifact). Provenance root is **cohort-dependent** (DP01–06 BLF-singles-rooted vs DP07+ GE-coinc-rooted) → per-cohort `derived_from`, not a fixed template. Shared index tables (~21 MB = 0.017 %) **duplicated freely** (content-addressing dedups at registry/cache). Columns annotated (#307) + optionally quantized (#310). Multi-block-same-`.tsra` reserved for always-together/never-fetched-apart; tight analysis doesn't force co-location (cross-object query spans artifacts) | **Proposed** |
| [0058](0058-generation-provenance-and-identity-inheritance.md) | Generation provenance + identity inheritance (#324, #342, landed on `dev` via #409 superseding stranded PR #346) — **deterministic, identity-bearing provenance is sealed; only wall-clock/host stays unsealed** (applies ADR-0042's principle correctly): (1) structured sealed `producer{tool,version,git_commit,git_repo,dirty}` (tessera stamps its own build); (2) sealed `generation{config \| config_ref}` — a **generic, non-opinionated bag** (any DAQ/SIM/recon fills its own keys; inline JSON or a `config_ref` pointer to a bit-faithful carried Blob block, e.g. a vendor `.cfg`); (3) **schema-enforced** at seal for `role=derived`/DAQ/SIM (block if producer/config absent); (4) **schema-driven seal-time identity inheritance** — DAG-walk `derived_from` copies the fields the *schema* marks `inherit` (a per-field axis alongside ADR-0040 sensitivity tiers — inherited fields carry their tier, so anonymize/crypto-shred and inheritance are one mechanism run in opposite directions) into sealed `metadata`; transform settings stay in `generation`, never inherited. Inheritable set + enforcement rule live in the schema, not the engine. Writer-determinism intact; one-time corpus regen (ADR-0049 precedent). Numbered 0058 (not 0052) after the original PR #346's ADR-0052 collided with the shipped ADR-0052 versioning-and-release | **Accepted** (implemented) |
| [0002](0002-concurrency-model.md) | **D2** concurrency — synchronous API; async kept dependency-internal (no tokio runtime); object_store deferred | **Accepted** (as-built) |
| [0003](0003-schema-identification.md) | **D3** schema id — string product names, open-world (no numeric allocator) | **Accepted** (as-built) |
| 0007 | **D7** encryption-at-rest — **non-goal**: delegate to storage-layer SSE / dm-crypt; no per-block envelope in the format | Proposed (decide by P1) |

D6 (bindings vs validation) is settled in the ROADMAP itself (P7/P8) and needs no separate ADR.
