---
type: pull_request
state: closed (merged)
branch: feat/bool-utf8-columns → dev
created: 2026-08-03T01:24:00Z
updated: 2026-08-03T22:48:55Z
author: gerchowl
author_url: https://github.com/gerchowl
url: https://github.com/vig-os/tessera/pull/354
comments: 2
labels: none
assignees: none
milestone: none
projects: none
relationship: none
merged: 2026-08-03T22:47:31Z
synced: 2026-08-04T10:43:00.782Z
---

# [PR 354](https://github.com/vig-os/tessera/pull/354) feat(table): Bool + Utf8 ColumnData — first-class boolean & string columns

Stacked on #353 → #350. Review those first.

`table::ColumnData` covered `I8…F64` only — no booleans, no strings. Any real
listmode table has both, so integrators encode them by convention: a bool becomes a
`u1` column, a small categorical becomes a `u1` dictionary code, and the convention
itself gets written into the product's sealed `extra`. That convention is invisible to
every other reader of the file, which defeats the point of a self-describing format.

Adds:

- `ColumnData::Bool(Vec<bool>)` — dtype code `b1`, stored bit-packed as a Vortex
  `BoolArray`.
- `ColumnData::Utf8(Vec<String>)` — dtype code `str`, stored as a Vortex
  `VarBinViewArray`.

Both are wired through every `ColumnData` site (`numpy_code`, `len`, `as_i64`,
`to_le_bytes`/`from_le_bytes`, `empty_column`, `slice`, `extend`, `to_vortex`, and the
decode dispatch), so they work in batch encode, streaming encode, chunk-index
construction, and the LE-bytes cross-runtime path alike.

**On strings and dictionaries:** raw `Utf8` with repeats is not wasteful — the
BtrBlocks compressor picks FSST/dictionary for a low-cardinality column on its own.
The test asserts this rather than assuming it: the same schema encoded with 300
repeated strings comes out smaller than with 300 unique ones. For a *known* small enum
the better schema is still an integer code column plus a code→label descriptor (the
string analogue of `Transform::Lookup`) — integer filtering, explicit enum-ness — but
that is a schema choice layered on these primitives, not a reason to withhold them.


---
---

## Comments (2)

### [Comment #1](https://github.com/vig-os/tessera/pull/354#issuecomment-5164175413) by [@gerchowl](https://github.com/gerchowl)

_Posted on August 3, 2026 at 08:43 AM_

CI caught a `workspace-fmt` failure — rustfmt diffs in test code I hand-wrote and never ran `cargo fmt` over. Fixed and squashed into the commit that introduced it, so the branch has no formatting-only commit. Verified `cargo fmt --all --check` is clean at **every** commit of the stack (#354 and all four of #355), not just at the tips, and 116/116 tessera-io lib tests still pass.

Worth noting the gate did its job: this is exactly the class of thing that would otherwise land and annoy the next contributor.

---

### [Comment #2](https://github.com/vig-os/tessera/pull/354#issuecomment-5170196162) by [@gerchowl](https://github.com/gerchowl)

_Posted on August 3, 2026 at 06:24 PM_

Status note before I lose my session (host reboot): **CI failed again** on run 30798428468 (both arches, ~22-24 min, so it got well past the fmt gate that failed last time). I could not isolate the failing check from `--log-failed` — the output is dominated by nix build/substitute lines and the actual failure marker did not surface in the tail I could grep.

Local state at this commit is green: `cargo fmt --all --check` clean at **every** commit of the stack, 116/116 tessera-io lib tests pass with `--test-threads=1`, clippy `-D warnings` clean.

So the failure is most likely something the local gate cannot reach — the workspace-wide build (`tessera-ingest`/`tessera-cli`, which need system libhdf5 I do not have) or a check that only runs under `nix flake check`. Whoever picks this up: open the run's web log directly rather than `gh run view --log-failed`, the marker is easier to find there.

#355 is rebased and queued behind this one.

---
---

## Commits

### Commit 1: [02df56f](https://github.com/vig-os/tessera/commit/02df56fc42c62937857936e56d55a6e2ae92865e) by [gerchowl](https://github.com/gerchowl) on August 1, 2026 at 12:31 PM
feat(table): Bool + Utf8 ColumnData — first-class boolean & string columns, 160 files modified (tessera/crates/tessera-io/src/table.rs)

### Commit 2: [f60046a](https://github.com/vig-os/tessera/commit/f60046adf0ffb20f7925bf1a66b0fe1b02ebdf93) by [gerchowl](https://github.com/gerchowl) on August 3, 2026 at 10:14 PM
fix(io,ingest,cli): cover Bool + Utf8 in every ColumnData match, 162 files modified (tessera/crates/tessera-cli/src/nav.rs, tessera/crates/tessera-cli/src/sql.rs, tessera/crates/tessera-ingest/src/ge_hdf5.rs, tessera/crates/tessera-io/src/table.rs)
