---
type: pull_request
state: closed (merged)
branch: perf/table-read-throughput → dev
created: 2026-08-03T01:24:31Z
updated: 2026-08-03T23:04:09Z
author: gerchowl
author_url: https://github.com/gerchowl
url: https://github.com/vig-os/tessera/pull/355
comments: 2
labels: none
assignees: none
milestone: none
projects: none
relationship: none
merged: 2026-08-03T22:48:38Z
synced: 2026-08-04T10:42:59.139Z
---

# [PR 355](https://github.com/vig-os/tessera/pull/355) perf(table): make reads fast — projection, pooled runtime, column-parallel materialise (3.2x)

Closes #352 — but **not** by doing what #352 proposed. See "The wrong answer" below.

Stacked on #354 → #353 → #350. The last commit is the substance; review that first
if you are short on time.

## The problem

A full-materialise `decode` of a wide listmode table ran ~4x slower than a mature
row store. The standing explanation was "Vortex decode throughput," which nobody
had measured.

## The wrong answer (what #352 asked for)

I filed #352 proposing that `decode` decompresses row-groups serially and should
fan `execute()` across the worker pool. Before building it, I profiled. The
premise was wrong, and the fix would have chased ~5% of the cost.

`examples/read_profile` (added here) peels a read apart on one blob. 2 M rows ×
21 columns, 31 row-groups, before this PR:

| stage | ms | delta |
|---|---:|---:|
| S0 scan, never `execute` | 2.1 | 2.1 |
| S1 + struct `execute` | 2.1 | 0.0 |
| S2 + field `execute` — **the real decompress** | 8.4 | 6.3 |
| S2b + read every decompressed value | 21.0 | 12.6 |
| S3 + copy into `ColumnData` = `decode()` | 128.0 | **107** |

Genuine Vortex decompress is ~5% of a read and the layout/scan path ~2%. **The
host copy is ~93%.** Parallelising `execute` would have bought ~6 ms out of 128.

S2b exists because "S2 is cheap" invites the objection that `execute` returned
something lazy; it touches every decompressed value and the cost stays put.

## The right answer

Two fixes, both following from that split:

1. **Pre-size the accumulators** (`empty_column_with_capacity`). They grew from
   empty, so each column was re-allocated and re-copied O(log n) times.
   Reserving the declared row count leaves one copy per row-group. The declared
   count is attacker-controlled for an untrusted blob, so it is clamped to
   `BLOCK_ROWS` — the partitioning law's maximum rows in one table block; a
   larger table still decodes, it just resumes amortised growth past the clamp.

2. **Fan the decompress-and-copy across cores, by column** (`materialise`).
   `decode` now drives the scan to a `Vec<StructArray>` (cheap — the structs are
   canonical but their fields are still *encoded* views into the blob the caller
   already holds, so the resident set does not grow) and hands them to a
   column-parallel fan-out. Each column is owned by exactly one worker walking
   the row-groups in order, so the output is bit-identical to serial. Tables
   under 2^20 values stay exactly as serial as before.

`decode_projected` and `decode_column` take the same path.

## Numbers

Interleaved A/B — the same example built at this commit and at its parent,
best-of-7, five alternating runs, 10-core box:

| | base | this PR | |
|---|---:|---:|---:|
| `decode` (2 M × 21) | 128–137 ms (med 130) | 38–42 ms (med 41) | **3.2x** |
| `decode_projected` (5 of 21) | 42–43 ms (med 42) | 12–18 ms (med 14) | **2.9x** |

Standalone thread-scaling of the materialise: 2.1x at 8 workers, flat past that —
the ceiling is memory bandwidth, not core count.

Downstream check on a real workload (PET singles listmode, 310 k rows, 21
columns, identical rows written both ways): tessera went from **4.0x slower than
parquet to 1.46x faster**, at 35.6% smaller on disk.

## Correctness

`materialise_is_bit_identical_across_thread_counts` drives the pinned-worker seam
at 1/2/3/4/8/64 workers (64 > column count, exercising the empty-group edge)
across every dtype — including the `Bool` and `Utf8` columns from #354, which
decode via their own path — and over repeated chunks to exercise the multi-chunk
append order. Every configuration is asserted byte-equal to the serial result.

## Also in this PR (earlier commits)

- `decode_projected` — multi-column projection pushdown in a single session,
  instead of N `decode_column` calls each paying session setup.
- `READ_RT` — reads drive the scan on a `CurrentThreadWorkerPool` sized to
  available parallelism, not the bare single-threaded `CurrentThreadRuntime`.
- Module docs (#351) telling integrators which runtime to use and that
  full-materialise-to-`Vec<struct>` is the slow path on purpose.

## Note for the maintainer

`array::tests::encode_emits_a_structured_compression_trace` fails in a threaded
`cargo test -p tessera-io --lib` run and passes both in isolation and under
`--test-threads=1` (116/116). It does so on `origin/dev` too — I verified before
and after — so it is **pre-existing and unrelated to this PR**. It does not affect
CI: `nix flake check` drives `cargo nextest run`, which gives each test its own
process, so CI is legitimately green. It looks like `tracing` callsite-interest
caching — another test reaches `array::encode` first with no subscriber installed,
the DEBUG callsite is cached globally as `never`, and the scoped `with_default`
subscriber then sees an empty log. Filed as #356 rather than smuggled in here.

Everything in this PR is green: 116/116 `tessera-io` lib tests, plus the new
thread-equivalence test.



---
---

## Comments (2)

### [Comment #1](https://github.com/vig-os/tessera/pull/355#issuecomment-5161537149) by [@gerchowl](https://github.com/gerchowl)

_Posted on August 3, 2026 at 01:57 AM_

Pushed two follow-ups after self-review:

**1. Fixed a `-D warnings` break I introduced.** The projection commit added a `thread_local!` block and left `runtime_session`'s doc comment stranded above the macro — `unused_doc_comments`, which would have failed the clippy gate. The comment now documents `runtime_session` again (where `dev` has it) and the thread-locals carry a plain `//` block. Squashed into the commit that introduced it, so every commit in the stack is independently clean: verified `cargo clippy -p tessera-io --lib --all-targets -- -D warnings` at each of the four commits, 0 errors each.

**2. Added `decode_with_workers(spec, blob, workers)`.** `decode` sizes its fan-out to `available_parallelism()`, which is right for one decode on an idle machine and wrong inside a caller's own parallel loop — N concurrent `decode`s would each spawn N workers. I had only documented that hazard; this makes it fixable. A cohort decode across a thread pool passes `1` and keeps parallelism at the outer level, where the work granularity is better anyway. The equivalence test covers both entry points.

Local gate: 116/116 `tessera-io` lib tests, clippy clean.

Note that the full-workspace suite cannot build outside the nix dev shell here (`hdf5-metno-sys` needs system libhdf5 via pkg-config), so `tessera-ingest`/`tessera-cli` are unverified locally — they don't touch anything this PR changes (only `table::encode` and `tessera-core` types), but CI is the real check.

---

### [Comment #2](https://github.com/vig-os/tessera/pull/355#issuecomment-5172566489) by [@gerchowl](https://github.com/gerchowl)

_Posted on August 3, 2026 at 10:49 PM_

Landed on `dev` as 70628de (and #354 as 794869a), gated on a green `nix flake check` — not GitHub CI.

**A real regression in this PR was caught and fixed before merge.** `tessera-py-import` panicked with `Attempted to use a Handle after its runtime was dropped`. Attribution was measured, not argued: building that same check on a clean `origin/dev` worktree returned exit 0, so it was not pre-existing.

Cause: `VortexSession` is `Arc`-backed, so `clone().with_handle(..)` rebinds *shared* state rather than yielding an independent session. This PR's cached `SESSION_TEMPLATE` was shared by all five entry points — so `encode`/`encode_streaming`/`decode_column`, each of which builds a `CurrentThreadRuntime` and drops it on return, left the long-lived pooled `READ_RT` session pointing at a dead runtime. Reproduced deterministically as pooled `decode` → `decode_column` → pooled `decode`.

Fix: build a session per runtime (`new_session`) and drop the template. No measurable cost — the read win is the worker pool, not session reuse (`read_profile` puts ~93% of a read in the host copy, and the earlier A/B recorded template caching as moving nothing).

**Why the suite could not catch it:** nextest runs a process per test, so it reported **345/345 green with the bug present**. The failure requires a pooled read, a short-lived-runtime read, and another pooled read in *one* process — only `tessera-py-import` does that. Added `short_lived_runtime_does_not_poison_pooled_session`, which reproduces that ordering in-process (nextest now 346).

Worth noting the mirror image: pre-existing #356 fails only under shared-process `cargo test --lib` and passes under nextest. The two isolation modes hide different bug classes.

---
---

## Commits

### Commit 1: [ffb66d0](https://github.com/vig-os/tessera/commit/ffb66d0911570d29a399ddd89d888360fc6800d6) by [gerchowl](https://github.com/gerchowl) on August 1, 2026 at 11:30 PM
perf(table): single-session multi-column projection (decode_projected) + cached session template, 80 files modified (tessera/crates/tessera-io/src/table.rs)

### Commit 2: [864632c](https://github.com/vig-os/tessera/commit/864632c745e9437eed802cfe841e87bdb5f46657) by [gerchowl](https://github.com/gerchowl) on August 2, 2026 at 12:10 PM
perf(table): drive table reads on a multi-core worker pool (READ_RT), not the single-thread runtime, 97 files modified (tessera/crates/tessera-io/src/table.rs)

### Commit 3: [f031c32](https://github.com/vig-os/tessera/commit/f031c32e9755d5444b305c8382691736e16db4a0) by [gerchowl](https://github.com/gerchowl) on August 2, 2026 at 01:08 PM
docs(table): surface the parallel read runtime + intended access pattern, 23 files modified (tessera/crates/tessera-io/src/table.rs)

### Commit 4: [4ad1cf4](https://github.com/vig-os/tessera/commit/4ad1cf40bdceabcb8d6137226e700bdbc9402a37) by [gerchowl](https://github.com/gerchowl) on August 3, 2026 at 01:08 AM
perf(table): column-parallel materialise + pre-sized accumulators — 3.2x on a full read, 612 files modified (tessera/crates/tessera-io/examples/read_profile.rs, tessera/crates/tessera-io/src/table.rs)

### Commit 5: [a8fa8df](https://github.com/vig-os/tessera/commit/a8fa8df7f6265fc1448a980930c06a8457a9f58f) by [gerchowl](https://github.com/gerchowl) on August 3, 2026 at 10:39 PM
fix(io): give every runtime its own session — a dropped one poisoned the pooled read, 81 files modified (tessera/crates/tessera-io/src/table.rs)
