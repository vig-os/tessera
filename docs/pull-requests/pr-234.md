---
type: pull_request
state: closed (merged)
branch: feat/blob-niceties → spike/tessera-core
created: 2026-06-30T17:22:53Z
updated: 2026-06-30T17:35:52Z
author: gerchowl
author_url: https://github.com/gerchowl
url: https://github.com/vig-os/tessera/pull/234
comments: 0
labels: none
assignees: none
milestone: none
projects: none
relationship: none
merged: 2026-06-30T17:35:51Z
synced: 2026-08-04T10:44:37.326Z
---

# [PR 234](https://github.com/vig-os/tessera/pull/234) feat(blob): cloud-aware extract + verify-while-pack + opt-in parallel blake3

The three blob follow-up niceties (built in parallel, integrated + gated together). Green `nix flake check`.

## A — cloud-aware `extract`
`tessera extract` now routes through `open_local_or_url`, so `s3://…` / `http(s)://` work under `--features cloud` (range-GETs just the block), with the local path unchanged. The stage-to-`.part` + atomic-rename-on-`Ok` safety is preserved byte-for-byte.

## B — verify-while-pack (closes the hash/pack double-read race)
`blob_ref_streaming` hashes the file, then `pack_streaming` re-reads it — a file change between the two would seal a digest that doesn't match the packed bytes (a silent DOA archive). New `pack_streaming_verified` re-hashes each fragment **as it copies** (shared loop with `pack_streaming` → byte-identical output) and returns `Error::Integrity` on mismatch. The blob ingest arm (`seal_streaming_to_tsra`) routes through it. **Corpus goldens untouched** (asserted by the conformance tests + a byte-equality test).

## C — opt-in parallel blake3
`digest_file_parallel` / `blob_ref_streaming_parallel` (16 MiB chunks → `update_rayon`) for CPU-bound multi-GB hashing. **Default path unchanged** (single-thread); callers opt in. blake3 `rayon` feature added to `tessera-io` only — **`tessera-core` (wasm) stays rayon-free** (verified via `cargo tree --target wasm32`). A determinism test asserts `serial == parallel == one-shot`.

Each was clippy-clean + crate-tested in isolation and re-verified combined; full gate green.


---
---

## Commits

### Commit 1: [a9d3c3b](https://github.com/vig-os/tessera/commit/a9d3c3bd7dd5ad9e1c7a84c9f5ede636c300e780) by [gerchowl](https://github.com/gerchowl) on June 30, 2026 at 05:03 PM
feat(cli): cloud-aware streaming extract via open_local_or_url, 34 files modified (tessera/crates/tessera-cli/src/main.rs)

### Commit 2: [ef0d4cf](https://github.com/vig-os/tessera/commit/ef0d4cfa4cfbaad1bda7da02d880be7cc27a401e) by [gerchowl](https://github.com/gerchowl) on June 30, 2026 at 05:05 PM
feat(io): verify-while-pack for streaming blob (close hash/pack double-read race), 250 files modified (tessera/crates/tessera-ingest/src/engine.rs, tessera/crates/tessera-io/src/blob.rs, tessera/crates/tessera-io/src/container.rs, tessera/crates/tessera-io/src/lib.rs)

### Commit 3: [77c7804](https://github.com/vig-os/tessera/commit/77c7804e019426591267a093fd85860ab9d3792a) by [gerchowl](https://github.com/gerchowl) on June 30, 2026 at 05:05 PM
feat(io): optional multithreaded blake3 for large-file blob hashing, 174 files modified (tessera/Cargo.lock, tessera/crates/tessera-io/Cargo.toml, tessera/crates/tessera-io/src/blob.rs)

### Commit 4: [32508f5](https://github.com/vig-os/tessera/commit/32508f5a1b697c5b8a2d8f1fe41a304858bd92cb) by [gerchowl](https://github.com/gerchowl) on June 30, 2026 at 05:12 PM
style: rustfmt the integrated blob-niceties, 54 files modified (tessera/crates/tessera-io/src/blob.rs, tessera/crates/tessera-io/src/container.rs, tessera/crates/tessera-io/src/lib.rs)

### Commit 5: [8a56a77](https://github.com/vig-os/tessera/commit/8a56a77d79b0050cdf9934a9563f072a9784b81a) by [gerchowl](https://github.com/gerchowl) on June 30, 2026 at 05:29 PM
address #234 review: stage+rename verified pack, drop double-buffer, doc nits, 57 files modified (tessera/crates/tessera-io/Cargo.toml, tessera/crates/tessera-io/src/blob.rs, tessera/crates/tessera-io/src/container.rs)
